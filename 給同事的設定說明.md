# 給同事的設定說明

這份文件教你把這套 **Ragic ERP 工具** 跑起來，並（選用）讓 AI agent（Claude Code 或 Codex）掛在這個資料夾幫你操作。

---

## 一、這是什麼

一套 Python 工具，把潮玩波普在 Ragic 上的日常作業自動化：

- **新建銷售單**：讀客戶（TRU / LE …）的訂購 Excel，自動在 Ragic 開銷貨單
- **建立出貨單 / 出庫單**：銷貨單一鍵拋轉
- **匯出庫存報表**：客戶現貨表、月度庫存金額表
- **在途查詢**：採購單未到貨
- **Agent mode**：用自然語言查庫存/銷售（唯讀分析，走 Anthropic API）

主程式：`app/ragic_upload.py`（互動式選單）。

---

## 二、第一次設定（約 5 分鐘）

### 1. 需要先有
- **Python 3.8 以上**（Mac 內建，或 `brew install python`）
- **Git**

### 2. 取得程式
```bash
git clone <這個 repo 的網址>
cd erp-tools-ragic
```

### 3. 放 Ragic API Key（最關鍵）
工具要靠這把 key 才能讀寫 Ragic。把 key 存成檔案：
```
~/.boptoys-ai_key
```
- 內容是 Ragic 帳號的 API Key（已是 Base64，直接整段貼進去，不用換行）。
- 取得方式：Ragic 右上角頭像 → 個人設定 → API Key；或直接跟 Woody 拿。
- 第一次跑工具若沒這檔，它也會跳出來請你貼上、並自動存好。

> ⚠️ **這把 key = 正式 ERP 的讀寫權限**。能改庫存、開單據。請勿外流、勿放進 git、勿貼到群組。離職/換機請通知 Woody 重置。

### 4. （選用）Agent mode 的 Anthropic Key
只有要用工具內建的「Agent mode（AI 數據分析）」才需要：
```
~/.boptoys-anthropic_key
```
存 OpenAI… 不，是 **Anthropic** 的 API Key（console.anthropic.com → API Keys）。按量計費。
不用 Agent mode 可略過。

### 5. 跑起來
```bash
./start.command
```
第一次會自動建 venv、裝套件（約 1 分鐘），之後直接進主選單。
> Mac 若擋執行：`chmod +x start.command` 後再跑。

---

## 三、日常怎麼用（互動選單）

跑 `./start.command` 後選功能：

| 選單 | 做什麼 |
|---|---|
| 新建銷售單 | 把客戶訂購檔放進 `client_order/<客戶代碼>/`（如 `client_order/TRU/`），它會自動掃描、逐張開單 |
| 建立出貨單 | 銷貨單拋轉出貨單 |
| 建立出庫單 | 出貨單拋轉出庫單（扣庫存） |
| 匯出庫存報表 | 客戶現貨表 / 月度庫存金額表 |
| 在途查詢 | 採購單未到貨查詢 |
| Agent mode | 自然語言查資料（唯讀） |

**開單前先把檔案放對資料夾**：資料夾名稱 = 客戶格式代碼（`TRU`、`LE`…），程式靠它選對應的解析器。

**第一個提示輸入 `debug`** 可進入預覽模式（不實際寫入 Ragic），先看會開出什麼單再決定。

---

## 四、（進階）讓 AI agent 掛在這資料夾幫你做事

這個 repo 可以讓 **Claude Code** 或 **Codex**（OpenAI）掛上來，直接幫你查庫存、開單、甚至修程式。

- **Claude Code**：用 Claude 訂閱（Pro/Max）或 Anthropic API
- **Codex**：用 ChatGPT 訂閱或 OpenAI API

兩者都能：讀寫這個資料夾的檔、跑指令、呼叫 Ragic API、修工具程式。你只要在對話框用中文交代，例如：

- 「這份 TRU PO 幫我查庫存夠不夠發」
- 「PO 53961 林口三井幫我開單」
- 「建一個客戶 TRU-XXXX」
- 「工具讀不到客戶新格式，幫我修」

**安全原則**：寫進正式 ERP 的動作（開單、改庫存、建客戶），agent 會**先把內容給你看、你確認後才送出**。

要讓 agent 動，那台電腦同樣需要：① 這個資料夾 ② `~/.boptoys-ai_key` ③ Python 環境。

---

## 五、卡住了找誰
- 程式問題 / API Key：找 Woody（tech@vertexdistributor.com）
- 客戶格式變動導致開單錯：直接叫 agent「幫我看 parser、修跑版」，或回報 Woody
